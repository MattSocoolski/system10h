name = "live-preview-api"
main = "src/index.js"
compatibility_date = "2024-12-01"

# KV namespace for rate limiting
[[kv_namespaces]]
binding = "RATE_LIMIT_KV"
id = "65b2952ce7f244aaaa14b25517cb9e7d"

# Local dev KV (wrangler dev --local)
# [[kv_namespaces]]
# binding = "RATE_LIMIT_KV"
# id = "PLACEHOLDER_PREVIEW_ID"
# preview_id = "PLACEHOLDER_PREVIEW_ID"

# Secrets (NEVER commit these — set via: wrangler secret put <NAME>):
# - ANTHROPIC_API_KEY
# - TURNSTILE_SECRET_KEY
# - MAILERLITE_API_KEY
# - MAILERLITE_GROUP_ID  (optional — hardcoded fallback: 179581509743674554)
# - RESEND_API_KEY
# - TELEGRAM_BOT_TOKEN   (optional — for style-match alerts)

# Route (set after first deploy):
# route = { pattern = "system10h.com/api/*", zone_name = "system10h.com" }
